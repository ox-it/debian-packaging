#!/usr/bin/make -f

%:
	dh $@ --with python2

override_dh_clean:
	dh_clean
	#rm django_hosts.egg-info versiontools* -rf

override_dh_auto_test:
	#django-admin test --settings=django_hosts.tests.settings --pythonpath=.

get-orig.tar:
	wget https://github.com/hiidef/oauth2app/archive/f8205e2146477224a666c03bf115ddf3ff7610ff.tar.gz -O ../python-oauth2app_0.3.0.orig.tar.gz
        tar xf ../python-oauth2app_0.3.0.orig.tar.gz --strip-components=1
